/* Auto-generated placeholders. Payload rewrites these files when the dev
 * server starts / on `payload generate:importmap`. They exist so the App
 * Router can compile before Payload's first boot. */
export const importMap = {}